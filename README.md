tp1:
Script principal (con menu de opciones): script.sh
El script consolidar.sh se copia cuando se crea el entorno en las opciones de script.sh.
El script env.sh tiene la variable de ambiente a configurar antes de iniciar la aplicacion.
